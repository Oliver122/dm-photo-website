# REQ-009 — two UI variants via Herdr

Parent: `feat/dm-analog-ingest`.

| Worker | Branch | Style |
|--------|--------|-------|
| ui-variant-a | `feat/dm-analog-ui-variant-a` | Film strip / darkroom |
| ui-variant-b | `feat/dm-analog-ui-variant-b` | Clear desk / light airy |

After both idle: open each worktree (or serve both), pick one, merge winner into parent.
Preview panel must stay **outside** `#analog-ingest-list` poll.
