# Review — REQ-006 / REQ-007 (film EXIF + import preview)

- **Date:** 2026-08-06
- **Status:** specs only (not implemented)
- **Branch:** `feat/dm-analog-ingest`

## Summary

Two planned follow-ons to accepted REQ-005:

1. **REQ-006** — User sets film ISO + Objektiv (`50mm` / `f/2.4`); stamp EXIF on the whole roll before PhotoPrism upload.
2. **REQ-007** — After ZIP extract, job enters `preview`; user rotates frames, then confirms import (then labeling + upload).

## Suggested implementation order

1. REQ-006 (form + columns + stamp) — smaller, fits current auto-pipeline.
2. REQ-007 (pause at preview + gallery + rotate + confirm) — changes job state machine.

## Notes

- Lens input: prefer separate numeric fields (mm + aperture), not a single free-text “50mm 2.4” parse in MVP (optional smart-parse later).
- Rotation must rewrite pixels/orientation on disk so PhotoPrism does not depend on browser CSS transforms.
