# PR review — REQ-015 Discord allowlist + gated admin (re-review)

**Date:** 2026-08-09 (re-review after fixes)  
**Branch:** `feat/req-014-lab-bench-ui` @ `c862a0f`  
**Base:** `main` (`ae36017`) — `git diff main...HEAD` non-empty (48 files)  
**Working tree:** clean  
**Reviewer:** senior PR readiness (verification pass)  
**REQ:** [REQ-015](../requirements/REQ-015-discord-allowlist-admin.md)

## Verdict

**Merge-ready** — prior blocker/high/medium product findings are fixed and covered by tests. Remaining items are **low/nit** doc drift and polish only.

`cargo test` → **84 passed**; `cargo test 015` → **15 passed**.

---

## Prior findings — verification

| Prior | Status | Evidence |
|-------|--------|----------|
| **blocker** Empty `main...HEAD` / untracked migration+handler | **Fixed** | Commit `c862a0f`; migration, `allowlist.rs`, partials, REQ docs in tree |
| **high** Boot seed demotes UI admins | **Fixed** | `upsert` ON CONFLICT: `is_admin = CASE WHEN excluded.is_admin = 1 THEN 1 ELSE discord_allowlist.is_admin END`; SQLite repro keeps `1`; seed uses same upsert |
| **high** Add/upsert bypasses last-admin | **Fixed** | Promote-only upsert; demote only via `set_*` / `delete_*`; `t_015_d`, `st_015_h` re-add without checkbox |
| **medium** Session survives allowlist revoke | **Fixed** | `AuthUser` / `AdminUser` / `load_current_user` re-check allowlist + `session.flush()`; `st_015_i` |
| **medium** Missing last-admin / CRUD tests | **Fixed** | `t_015_d/e`, `st_015_g/h/i` |
| **medium** Non-numeric env seed / `.env` placeholder | **Fixed** | `is_discord_snowflake_id` skip+WARN; `.env.example` empty + comment example; `t_015_e` |
| **low** HTMX errors wipe list | **Fixed** | `list_with_feedback` keeps `#allowlist-list` + OOB `#allowlist-out`; `st_015_g` asserts list kept |
| **low** README public `/admin/login` | **Fixed** | README auth=`user`, allowlist routes + fail-closed notes |
| **medium** `ARCHITECTURE` / `REQ-008` / `FRONTEND-SURFACE` drift | **Open → low** | Still stale (see remaining); canonical REQ-015 / ROUTES / INDEX / README are correct — no runtime impact |
| **nit** Mixed REQ-014+015 branch | **Open (nit)** | Unchanged; OK if PR title/body call out both |

---

## Acceptance criteria (REQ-015)

| Criterion | Met? |
|-----------|------|
| Table + env seed (numeric only) | Yes |
| OAuth before upsert; denied → `?denied=1`; no `users` row | Yes |
| Empty allowlist → deny all | Yes |
| `AdminUser` = Discord + (password ∨ allowlist admin) | Yes |
| `/admin/login` requires Discord first | Yes |
| Dashboard CRUD + last-admin guard | Yes |
| Revoke allowlist clears session | Yes (beyond original AC; good) |
| `changeme` WARN + banner | Yes |
| Compose mirrors env | Yes |

---

## Remaining findings

| Severity | Location | Finding | Fix (optional follow-up) |
|----------|----------|---------|--------------------------|
| **low** | `.ai/ARCHITECTURE.md` L37–39 | Auth model still says admin login is public / admin flag “Independent of Discord login.” | Rewrite to Discord-first + password/allowlist capability. |
| **low** | `.ai/requirements/REQ-008-system-tests.md` | AC/table still claim `GET /admin/login` → 200 and anonymous `/admin` → `/admin/login`. Tests already redirect to `/login`. | Sync REQ-008 with ST-008-c/f. |
| **nit** | `.ai/FRONTEND-SURFACE.md` L227–233 | Says add → `#allowlist-out`; form actually `hx-target="#allowlist-list"` with OOB notice on `#allowlist-out`. | Document both targets. |
| **nit** | `allowlist.rs` | Last-admin errors still match `err.to_string().contains("last allowlist admin")`. | Typed/stable error if touching again. |
| **nit** | Branch / PR | Single commit mixes REQ-014 UI + REQ-015 auth; 1 commit ahead of `origin` (not pushed). | Push before opening PR; title should mention both REQs. |

No **blocker** / **high** / **medium** remain.

---

## Residual risks (accepted)

1. Last-admin count-then-update is TOCTOU; SQLite write serialization makes this impractical — not merge-blocking.
2. Empty allowlist at deploy is fail-closed by design; operators must set `DISCORD_ALLOWLIST` (documented).
3. OAuth gate covered via `POST /__test/discord_login/:id` (no live Discord E2E).
4. `Session` `Secure=false` — pre-existing HTTPS follow-up.

---

## Commands run

```bash
git status                          # clean; 1 commit ahead of origin
git log main..HEAD --oneline        # c862a0f
git diff main...HEAD --stat         # 48 files
cargo test                          # 84 passed
cargo test 015                      # 15 passed
sqlite3 :memory:                    # promote-only upsert keeps is_admin=1
```
